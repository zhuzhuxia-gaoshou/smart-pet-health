# ☀️ 晨间验收清单 v2（2026-09-18 晨）

> 昨夜今晨共 **11 个新提交**（`9991988` → `24c43c0`，未 push）。主线 M1~M5 全做 + 加餐 9 项全做。
> 自动化已全绿：冒烟 **156/156** · 欢迎页 **10/10** · pytest **9/9** · eval 路由 **26/26** + LLM 兜底实测 **5/5**。
> 浏览器整晚未开（用户要求），以下为需要真人过目的界面/交互验收项，按优先级排列，预计 25 分钟。

## 一、环境说明（先看）

- ⚠️ **数据库已重建**：昨晚开工时库内只剩「翠翠」和一只测试宠物「11」（无 id=1，冒烟会崩），已删库重建种子（可乐/布丁/翠翠，id 1~3）+ 完整演示数据（记录/用药/回忆/花费 15 笔/饮食 13 条）。当时的「11」没有恢复（演示库，判定无需保留）。
- 服务未在后台运行：双击 `启动.bat` 或 `cd backend && .venv\Scripts\python.exe main.py`。
- 「更多」菜单新增了「导出花费 CSV」，注意别和原有两项混。

## 二、新栏目验收（今天最重要的部分）

### 记账页（顶栏「记账」）
- [ ] 月份 ‹ › 切换、翻到别月出现「回到本月」；本月合计卡金额与流水能对上
- [ ] 分类条形图 5 行（医疗/粮食/用品/洗护/其他），**鼠标悬停第一行**：提示卡出现且**不探出卡片顶边**（昨夜修过一处溢出）；键盘 Tab 到行上也有提示卡，按 Esc 可收起
- [ ] 流水按宠物分组（可乐/布丁/翠翠/家庭共同），点行打开编辑弹窗；金额改成分数（如 12.345）保存应被中文提示拦下
- [ ] 「＋ 设月度预算」→ 设 800 → 合计卡出现进度条与「还剩/超支」徽章；把预算设为低于合计 → 琥珀「超支」徽章出现；设 0 清除
- [ ] 空月（翻到 2025 年）出现「还没有记账」空态
- [ ] AI：问「可乐这个月花多少钱」「今年医疗开销大头是什么」→ 健康分析师回答带真实金额

### 健康日历（顶栏「日历」）
- [ ] 月历今天高亮（实色圆数字）；布丁疫苗逾期日有红点、近两三天临期有琥珀点、长期驱虫滴剂整月紫点
- [ ] 点今天 → 当日事项卡：提醒分组（带「✓ 完成」）、用药分组、已做分组；点「✓ 完成」→ 卡片刷新重开（有重复周期的会提示下一轮日期）
- [ ] 点「查看宠物」跳详情；「＋ 健康记录」打开记录表单
- [ ] ‹ › 翻月、回到今天；键盘 Tab 到日期格 Enter 可开当日卡，Tab 顺序不乱（非本月格不可聚焦）
- [ ] AI：问「可乐和布丁谁更乖？」（需 `LLM_ROUTE=1` 才走 LLM 兜底，默认关闭时走通用助手，回答都应正常）

### 饮食日志（宠物详情 →「饮食」页签）
- [ ] 页签顺序：时间线/体重/用药/**饮食**/护理/资料；今日有记录时页签显示「饮食 · N」
- [ ] 小结卡（今日 N 顿/本周 M 次）；四色徽章（干粮橙/湿粮青/零食琥珀/生骨肉红——生骨肉是特意加深过的 AA 色）
- [ ] 「＋ 记一餐」增改删流程；**故意断网或停后端再进详情页**：饮食区应显示「加载失败 + 重试」而不是伪装的空态
- [ ] AI：问「可乐最近在吃什么」→ 健康分析师报今日/本周顿数与最近流水；问「可乐最近在吃什么**药**」→ 应回答用药而非饮食

### 症状分诊（AI 助手页头「🩺 症状速查」）
- [ ] 弹窗三段：选宠物 chip 单选 → 症状 chip 多选（按物种变化：可乐=犬病清单、翠翠=鸟病清单）→ 补充说明 + 可编辑预览
- [ ] 未选宠物/症状时按钮置灰；全选后预览文本含「宠物名（物种）出现以下症状：…」；手改预览后再换宠物，预览应**重新生成**（不残留上一只的文本）
- [ ] 点「生成描述并提问」→ 弹窗关、跳 AI 对话、自动发送；回答尾部有「以兽医意见为准」

## 三、加餐验收（快扫）

- [ ] 更多菜单「导出花费 CSV」下载打开中文不乱码，金额两位小数
- [ ] 健康记录页按月份分组（「2026 年 9 月 · N 条」组头）+ 底部「本月新增 N 条」合计行
- [ ] AI 今日简报（仪表盘）——**待实测**：真 Key 下简报应含一句本月花销概览（规则模式已有此行，LLM 版本昨晚未等到缓存刷新验证）
- [ ] 欢迎页（`/?welcome` 强制重看）CTA 下方出现「已守护 3 只宠物 · N 条健康记录 · M 项待处理」统计条
- [ ] 记账页签在 1280px 宽度下顶栏 6 页签不挤（昨晚按代码估的，值得肉眼确认）
- [ ] 暗色模式全屏走一遍记账/日历/饮食/分诊（圆点、徽章、条形图颜色都应自动切换且可辨）
- [ ] PWA：Chrome 地址栏出现安装图标（manifest + 爪印图标；无 Service Worker，属计划范围）

## 四、待实测（自动化覆盖不到，需真 Key 或真环境）

| 项 | 说明 |
|---|---|
| AI 简报花销句 | 真实 LLM 下简报是否调用 query_expenses 并自然带出花销（缓存 1 天+签名，可点「刷新」重生成） |
| LLM 路由兜底 | 默认关闭。想试：`.env` 加 `LLM_ROUTE=1` 重启，问「我该给可乐买什么牌子的狗粮？」回答标签应是护理顾问（此前会落通用助手）；关闭时行为与旧版逐字节一致 |
| 症状分诊 → AI 回答质量 | 分诊只负责组装提问，回答质量取决于模型；确认宠物档案越全回答越准即可 |
| 通知/提醒 | 浏览器通知开关与新到期弹窗（昨晚未重测） |

## 五、已知事项与取舍（都记录在案，无需操作）

- 路由词「第一次」会把「第一次吃生骨肉是什么时候」送到报告撰稿人（存量词，收窄有回归风险，未动）
- qwen3.8-flash 分类偶发空输出（思考块吃 token）：已用 max_tokens=512 + 空输出重试一次缓解，仍有极小概率失败回落 general
- `.diet-row` 不做 hover 位移、不进键盘委托（不可点行，位移会误导）；已建议补记进设计决策文档
- 编辑花销弹窗的「宠物」若数据库被手改出白名单外的脏值，打开会静默显示第一项「干粮」（饮食）/「医疗」（记账分类不会，有校验）——只能直改数据库造成，风险极低
- LLM 路由兜底默认 `LLM_ROUTE=0`：开启后每条规则未命中的闲聊最多多等 6s（分类），连续失败 3 次自动停用

## 六、如果想回滚

- 单项回滚：`git revert <commit>`（每个功能独立提交，互不纠缠）
- LLM 路由兜底：删 `.env` 里 `LLM_ROUTE=1`（或保持没写）即可，代码留着无害
- 全部推倒：`git reset --hard 762741c`（回到昨晚验收通过的点）——**不推荐**，会丢掉全部夜间成果

## 七、提交清单（`git log --oneline -11`）

```
24c43c0 test: pytest 后端核心单测（9 例）
d2b29c7 feat: PWA 基础可安装
04a5873 feat: 健康记录页按月份分组
4e83bc4 feat: 欢迎页真实数据统计条
caed494 feat: LLM 路由兜底分类（默认关闭，实测 5/5）
d52cfa8 feat: 记账加餐四件套（CSV/合计行/简报花销/预算）
78ec43c feat: 新栏目界面统一（门禁+无障碍 12 项修复）
38c13a0 feat: 症状分诊向导
c781dd9 feat: 饮食日志
bb48fdd feat: 健康日历栏目
9991988 feat: 花费记账栏目
```

验收通过后想推送：13+11=24 个提交都在本地，说一声「push」即可。
