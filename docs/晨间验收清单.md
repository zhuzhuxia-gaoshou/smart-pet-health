# ☀️ 晨间验收清单（请在浏览器里人工验证）

> 服务：双击 `启动.bat` 或 `cd backend && .venv\Scripts\python.exe main.py` → http://127.0.0.1:8000/app
> 浏览器可能有缓存，页面已带 no-cache 头，但保险起见 **Ctrl+F5 强刷一次**。
> 建议先在「⋯ 更多菜单 → 清空全部数据并重建示例」重置一次（现在需要**输入「清空」二字**确认），能看到全部新种子（重复周期、用药示例）。
> 欢迎页现在是回访直达：第二次访问会直接进应用；想重看过渡动画访问 `/?welcome`。

## 一、体重趋势图增强（档案详情 → 体重趋势页签）

- [ ] 图表上方出现分段控件「全部 / 近1年 / 近90天 / 近30天」，点击切换后折线与统计行同步变化，当前项白底橙字
- [ ] 鼠标在图上左右移动：竖直参考线跟随、最近的数据点放大高亮、上方浮出深色提示卡（体重 + 日期 + 较上次差值）；移出图表提示卡淡出
- [ ] 图表下方 4 个统计块：最新（带日期）/ 区间最低 / 区间最高 / 区间平均；翠翠（0.04kg）应显示两位小数
- [ ] 切到「近30天」若数据不足两条，显示"该区间内体重记录不足两条"提示而不是空白
- [ ] 「✦ AI 解读」按钮点击后变成圆形 spinner（不再是文字"解读中…"），完成后恢复
- [ ] **切暗色模式**（顶栏月亮图标）再悬停：提示卡应是浅底深字，可读

## 二、提醒重复周期（仪表盘 / 到期提醒 / 档案详情）

- [ ] 仪表盘「到期提醒」每行右侧有「✓ 完成」小按钮；悬停行时按钮变清晰；点它**不会**跳转详情页
- [ ] 点击可乐「体内外驱虫」的完成（它是每月重复）：toast 提示"已完成，下一轮已排到 YYYY-MM-DD"，该行从提醒消失，进可乐详情时间线能看到**新增一条今天日期的驱虫记录**，带「🔁 每月」和新的下次日期
- [ ] 点击一条**不重复**的提醒（如布丁「外耳炎就诊」）完成：toast "已标记完成"，行消失，时间线原记录保留但不再有到期徽标
- [ ] 新增/编辑健康记录弹窗：「下次日期」旁多了「重复周期」下拉（不重复/每天/每周/每月/每年）；编辑一条记录把周期改回"不重复"能保存成功
- [ ] 时间线里带下次日期的记录多了「✓ 标记完成（并排下一轮）」文字按钮
- [ ] 逾期提醒完成后，下一轮从**今天**起算（不是从原逾期日）——可用一条逾期的每周提醒验证

## 三、用药方案（档案详情 → 「用药 · N」页签）

- [ ] 页签标题显示在用数量（可乐应为「用药 · 2」，布丁「用药 · 1」，翠翠只显示「用药」）
- [ ] 在用药卡片：药名 + 剂量·频次，有结束日期的显示紫色疗程进度条与"剩 N 天"/"已超出计划 N 天"（布丁耳药水应显示已超出）；长期用药显示"长期 / 未设结束日期"
- [ ] 「＋ 添加用药」弹窗：药名必填，频次输入框有下拉建议（每日一次…），开始日期默认今天；保存后卡片出现、页签计数 +1
- [ ] 「✓ 结束用药」→ 移入「已结束」分组；「编辑」可改状态；「删除」抖动后移除
- [ ] 删光某宠物的用药后出现空状态卡（💊 图标 + 引导文案 + 「添加第一条用药」按钮）
- [ ] AI 助手问「可乐现在在吃什么药」应返回在用药物明细（若 AI 供应商不可用，示例模式也会命中关键词返回）

## 四、界面微交互（全站）

- [ ] 所有按钮：hover 只变亮不上浮，按下有轻微缩小；Tab 键盘导航时有 2px 橙色焦点环
- [ ] 卡片（统计卡/宠物卡/回忆卡）hover 统一上浮 2px + 深阴影
- [ ] 弹窗关闭有 150ms 淡出（不再"闪没"）；快速连开连关不出现卡死
- [ ] 顶栏「⋯」下拉菜单打开有轻微下落淡入
- [ ] 删除确认弹窗的红色按钮样式保持（现在是 solid-danger 类而非内联样式）
- [ ] 暗色模式下欢迎横幅渐变、爪印、文字颜色与之前一致（已收编为 token）
- [ ] 系统开启「减弱动态效果」时所有动画应瞬时完成

## 五、后端健壮性（可选：用浏览器地址栏或 curl）

- [ ] `/api/records?type=vaccine&limit=1` 返回 1 条 + total
- [ ] 新增记录时故意输错日期（如 2026/13/45）→ toast 显示"next_date：日期格式应为 YYYY-MM-DD"而不是静默成功

## 六、界面美化与微交互（本轮重点）

- [ ] 主按钮颜色略微加深（#C2703D→#A85A2E 系，为了白字对比度达标）——确认视觉可接受；边框/图标/图表/渐变仍是品牌陶土橙
- [ ] **暗色模式下主按钮/用户气泡变成浅橙底深字**（对比度 2.72→6.65 的修复），确认观感
- [ ] 状态色略微加深（琥珀/紫/绿），提醒的"剩 N 天"琥珀字应更清晰
- [ ] 欢迎页：第二次访问直接进应用；点「开始使用」后过渡动画中**点任意处或按 Esc 可直接进入**；按钮圆角与主应用一致（8px）、底色一致（#FBFAF7）
- [ ] AI 助手页副标题不再出现 LangChain/百炼 等技术词；每条 AI 回答的标签显示专家身份（AI · 健康分析师 / 护理顾问 / 报告撰稿人）——问「最近有哪些到期事项」→ 健康分析师；「布丁能吃西瓜吗」→ 护理顾问；「生成可乐的健康报告」→ 报告撰稿人
- [ ] 问「翠翠该打什么疫苗」：AI 应温和指出鸟类不接种常规疫苗（物种边界路由）
- [ ] 对 AI 说「帮我记一笔：可乐今天打了狂犬疫苗」应弹出确认草稿卡（和以前一致，但现在由护理顾问处理）

## 七、无障碍（键盘/读屏，可选验证）

- [ ] **纯键盘走一遍**：Tab 从地址栏进入页面 → 第一处应出现「跳到主内容」链接 → 回车后焦点进主内容；Tab 能到达提醒行/记录行/宠物卡/下拉菜单项，Enter 或空格触发；焦点处有橙色环
- [ ] 打开任意弹窗：焦点自动进入第一个输入框（删除确认这类无输入的弹窗焦点落在弹窗上）；**Tab 不会跑到弹窗外面**；关闭后焦点回到触发按钮
- [ ] 系统开启「减弱动态效果」：打开欢迎页 → 过渡层约 0.3 秒即跳转，不再干等 2 秒；仪表盘数字不滚动直接显示
- [ ] 时间线每条记录的「编辑/删除」小按钮在鼠标悬停该条时才出现（触屏设备常显）——确认没影响操作

## 待实测（AI 供应商相关）

- [ ] AI 对话调用 `query_medications` 工具的真实效果（问「可乐在吃什么药」）
- [ ] 健康分析回答里出现"在用药物 N 项"与"超出计划疗程"提示
- [ ] 多专家降级链：如百炼临时限流，回答仍能出来（走通用 Agent 或示例模式），前端不报错

## 已知设计取舍（不需要验收，仅告知）

- 提醒「完成」对不重复的事项只清除到期日（不在时间线新增"做了"的记录）；如希望完成也落一条记录，说一声即可改
- 每月/每年重复的月末钳制后按钳制日继续滚动（1/31→2/28→3/28），不追求回到原日号
- 「清空全部数据并重建示例」仍常驻更多菜单（文案已纠正+需输入确认）；若希望彻底移出主菜单请告知
